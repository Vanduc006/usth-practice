3. 
Character streams is high level for Input/Ouput (I/O) component for reading, writing text data
Processing data in 16-bit Unicode Characters

Primary class for manipulate character streams
// Reader
BufferReader, CharArrayReader, StringReader, InputStreamReader, PipeReader, FilterReader
// Writer same with Reader 

4.
Standard for I/O is 16bit Unicode
// Input
Scanner scanner = new Scanner(System.in);
String name = scanner.nextLn(); etc
// Output
System.out.println();
