#include <stdio.h>
int main() {
    int a;
    scanf("%d",&a);
    // printf("%d",a);
    switch (a) {
        case 1:
            printf("Monday");
            break;
        default :
            printf("hieu ngu");
            break;
      }
}